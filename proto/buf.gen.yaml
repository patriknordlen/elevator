version: v1

managed:
  enabled: true
  go_package_prefix:
    default: github.com/einride/elevator/proto/gen/go
    except:
      - buf.build/googleapis/googleapis

plugins:
  - name: go
    out: gen/go
    opt: module=github.com/einride/elevator/proto/gen/go
